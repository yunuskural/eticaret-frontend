/*
export class CustomHttpClientService{

  post(url,data,callback,errorCallback){
   post(url, data).then(callback).catch(errorCallback ? errorCallback : err => {
      console.log(err)
    })
  }

  get(axiosInstance,url, callback, errorCallback) {
    axiosInstance.get(url).then(callback).catch(errorCallback ? errorCallback : err => {
      console.log(err)
    })
  }

  put(axiosInstance,url, data, callback, errorCallback) {
    axiosInstance.put(url, data).then(callback).catch(errorCallback ? errorCallback : err => {
      console.log(err)
    })
  }

  delete(axiosInstance,url, callback, errorCallback) {
    axiosInstance.delete(url).then(callback).catch(errorCallback ? errorCallback : err => {
      console.log(err)
    })
  }

}
*/
